<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reja</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body style="background-color: #b8bbd1;">
    <div class="container">
        <h1 class="display-4 text-center py-1">Reja va Maqsadlar</h1>
        <div class="jumbotron p-3 shadow-sm">
            <form id="create-form" action="/create-item" method="post">
                <div class="d-flex align-items-center">
                    <input type="text" name="reja" id="create-field" autofocus autocomplete="off"
                        class="form-control mr-3" style="flex: 1">
                    <button class="btn btn-primary" style="border-radius: 20px; margin-left: 10px;">
                        Add new plan
                    </button>
                </div>
            </form>

        </div>
        <ul class="list-group pb-5" id="item-list">
            <% items.map(item=> { %>

                <li class="list-group-item list-group-item-info d-flex align-items-center justify-content-between">
                    <span class="item-text">
                        <%=item.reja%>
                    </span>
                    <div>
                        <button data-id="<%=item._id%>" class="delete-me btn btn-danger btn-sm">Delete</button>
                        <button data-id="<%=item._id%>" class="edit-me btn btn-secondary btn-sm mr-1">Edit</button>
                    </div>
                </li>
                <% }) %>
        </ul>
        <button class="btn btn-danger" id="clean-all" style="border-radius: 20px;">Delete all plans</button>
    </div>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src="/browser.js"></script>
</body>

</html>